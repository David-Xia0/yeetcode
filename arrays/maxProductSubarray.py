from typing import List

class Solution:
    def maxProduct(self, nums: List[int]) -> int:

        neg_count = 0
        zero_count = 0
        for i in range(len(nums)):
                 
        return max(nums)